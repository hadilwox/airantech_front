# Local dev (see .env, already checked in with this value):
# VITE_API_URL=http://localhost:8000

# Production build — set via .env.production (gitignored, never commit real
# values) or an environment variable injected by the build/CI system:
VITE_API_URL=https://api.airanacademy.ir
